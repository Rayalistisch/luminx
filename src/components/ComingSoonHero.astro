<section class="cs-hero" data-bg-color="#FFD6E8">
  <div class="cs-hero-bg"></div>

  <div class="cs-hero-shapes" aria-hidden="true">
    <div class="cs-shape cs-shape--1"></div>
    <div class="cs-shape cs-shape--2"></div>
    <div class="cs-shape cs-shape--3"></div>
    <div class="cs-shape cs-shape--4"></div>
  </div>

  <svg class="cs-hero-grid" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <defs>
      <pattern id="cs-grid" width="60" height="60" patternUnits="userSpaceOnUse">
        <path d="M 60 0 L 0 0 0 60" fill="none" stroke="rgba(255,255,255,0.06)" stroke-width="1"/>
      </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#cs-grid)" />
  </svg>

  <div class="cs-hero-content">
    <p class="cs-hero-label">DIGITAL MARKETING SOLUTIONS</p>
    <h1 class="cs-hero-title">LUMINX</h1>
    <p class="cs-hero-subtitle">Coming Soon</p>
  </div>

  <div class="scroll-indicator">
    <span></span>
    <p style="opacity: 0.5; font-size: 0.8rem; color: var(--white);">SCROLL</p>
  </div>
</section>

<style>
  .cs-hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    background: var(--black);
  }

  .cs-hero-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      var(--black) 0%,
      #1a0a1a 25%,
      #0a1a1a 50%,
      #1a1a0a 75%,
      var(--black) 100%
    );
    background-size: 400% 400%;
    animation: cs-gradient-shift 15s ease-in-out infinite;
    z-index: 0;
  }

  .cs-hero-grid {
    position: absolute;
    inset: 0;
    z-index: 1;
    opacity: 0.4;
    animation: slowZoom 20s ease-in-out infinite alternate;
  }

  .cs-hero-shapes {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }

  .cs-shape {
    position: absolute;
    border-radius: 50%;
    opacity: 0.15;
    filter: blur(80px);
  }

  .cs-shape--1 {
    width: 400px;
    height: 400px;
    background: var(--pastel-pink);
    top: 10%;
    left: 15%;
    animation: float 15s ease-in-out infinite;
  }

  .cs-shape--2 {
    width: 300px;
    height: 300px;
    background: var(--pastel-blue);
    top: 60%;
    right: 10%;
    animation: float 18s ease-in-out infinite reverse;
  }

  .cs-shape--3 {
    width: 250px;
    height: 250px;
    background: var(--pastel-purple);
    bottom: 20%;
    left: 5%;
    animation: float 12s ease-in-out infinite 3s;
  }

  .cs-shape--4 {
    width: 350px;
    height: 350px;
    background: var(--pastel-mint);
    top: 5%;
    right: 25%;
    animation: float 20s ease-in-out infinite 5s;
  }

  .cs-hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: var(--white);
  }

  .cs-hero-label {
    font-family: 'Inter', sans-serif;
    font-size: clamp(0.75rem, 1.2vw, 1rem);
    font-weight: 400;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    opacity: 0.6;
    margin-bottom: 1.5rem;
    animation: fadeInUp 1.2s cubic-bezier(0.4, 0, 0.2, 1) 0.2s backwards;
  }

  .cs-hero-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(5rem, 15vw, 14rem);
    font-weight: 800;
    line-height: 0.9;
    letter-spacing: -0.04em;
    margin-bottom: 1.5rem;
    background: linear-gradient(135deg, var(--white) 0%, var(--pastel-pink) 50%, var(--pastel-blue) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: fadeInUp 1.2s cubic-bezier(0.4, 0, 0.2, 1) backwards;
  }

  .cs-hero-subtitle {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.2rem, 2.5vw, 2rem);
    font-weight: 400;
    opacity: 0.7;
    letter-spacing: 0.05em;
    animation: fadeInUp 1.2s cubic-bezier(0.4, 0, 0.2, 1) 0.4s backwards;
  }

  @keyframes cs-gradient-shift {
    0%, 100% { background-position: 0% 50%; }
    25% { background-position: 100% 0%; }
    50% { background-position: 100% 100%; }
    75% { background-position: 0% 100%; }
  }

  @media (max-width: 768px) {
    .cs-hero {
      height: 100svh;
    }

    .cs-hero-title {
      font-size: clamp(3.5rem, 12vw, 5rem);
    }

    .cs-hero-label {
      margin-bottom: 1rem;
      font-size: 0.7rem;
    }

    .cs-hero-subtitle {
      font-size: 1.1rem;
    }

    .cs-shape {
      filter: blur(60px);
      opacity: 0.1;
    }
  }
</style>
